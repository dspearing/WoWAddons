<Bindings>
	<!-- window commands -->
	<Binding name="TOGGLEFISHINGBUDDY_TRK">
		if ( keystate == "down" ) then
			FBEnvironment.ToggleFishingBuddyFrame("FishingTrackingFrame");
		end
	</Binding>
</Bindings>